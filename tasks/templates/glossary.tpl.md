<!-- This file was auto-generated by `gulp glossary`. Don't edit this file! -->
# 用語集

「マギハッカーの異世界ベンチャー起業術」の用語集です。また、小説内で解説された技術用語も逆引きで含みます。

[索引](#索引) / <%- navigation %>

## 索引

| 行 | 用語 |
| -- | ---- |
<% toc.forEach(o => { %>| `<%= o.category %>` | <%- o.definitions.map(def => def.link).join(" / ") %> |
<% }) %>

<% toc.forEach(o => { %>
## <%- o.category %>

<%- o.definitions.map(def => def.link).join(" / ") %>
<%   o.definitions.forEach(def => { %>
<%= def.anchor %>
### <%= def.header %>

<%= wordLink(def.content) %>
<%     if (def.first) { %>
初出: <%- def.first.link %>

> <%= wordLink(def.first.text) %>
<%
       }
     })
   })
%>
